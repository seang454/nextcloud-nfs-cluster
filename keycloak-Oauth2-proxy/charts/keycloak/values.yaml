enabled: true
production: true   # Set to true for production mode ("start"), false for dev ("start-dev")

image:
  repository: quay.io/keycloak/keycloak
  tag: "26.7.1"
  pullPolicy: IfNotPresent

admin:
  username: admin
  password: "CHANGE_ME_KEYCLOAK_ADMIN"
  existingSecret: ""

db:
  enabled: true
  vendor: "postgres"
  url: "jdbc:postgresql://keycloak-oauth2-postgres:5432/keycloak"
  username: "keycloak"
  password: "CHANGE_ME_POSTGRES_PASSWORD"
  existingSecret: ""

service:
  type: ClusterIP
  port: 8080

proxy:
  headers: "xforwarded"
  hostname: "keycloak.seang.shop"
  hostnameStrict: "false"

traefik:
  enabled: true
  domain: "keycloak.seang.shop"
  tls:
    enabled: true
    secretName: "keycloak-tls"

resources:
  requests:
    cpu: 300m
    memory: 1.5Gi
  limits:
    cpu: "1.5"
    memory: 2.5Gi

autoscaling:
  enabled: false
  minReplicas: 2
  maxReplicas: 5
  targetCPUUtilizationPercentage: 75

realmImport:
  enabled: true
  realmName: "enterprise-realm"
  clientId: "oauth2-proxy"
  clientSecret: "Secret-Keycloak-OAuth2Proxy-2026-ChangeMe"

