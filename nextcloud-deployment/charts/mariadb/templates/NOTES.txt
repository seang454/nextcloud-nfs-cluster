MariaDB StatefulSet: {{ include "mariadb.fullname" . }} ({{ .Values.replicaCount }} replica(s))
Service (in-cluster DNS): {{ include "mariadb.fullname" . }}:3306
Database: {{ .Values.auth.database }} / User: {{ .Values.auth.username }}
